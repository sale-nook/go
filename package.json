{
	"name": "aws-appsync-go",
	"private": true,
	"version": "0.1.0",
	"description": "A simple budgeting tool",
	"packageManager": "yarn@3.2.1",
	"workspaces": [
		"./web/"
	],
	"scripts": {
		"test": "cd web/ && yarn test",
		"dev": "yarn concurrently \"node ./web/ngrok.js\" \"yarn workspace webapp dev\" -names 'ngrok webapp'"
	},
	"devDependencies": {
		"concurrently": "^7.3.0",
		"ngrok": "^4.3.1"
	}
}
