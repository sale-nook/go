{
	"name": "@davemackintosh/cdk-appsync-go",
	"private": true,
	"version": "1.0.0",
	"description": "Golang serverless application featuring Cognito, user pools, user identity pool, dynamoDB + KMS, CDK (in golang) with a Next JS/TypeScript frontend + Apollo and GraphQL Codegen/linting",
	"packageManager": "yarn@3.2.1",
	"workspaces": [
		"./web/"
	],
	"scripts": {
		"test": "cd web/ && yarn test",
		"dev": "yarn concurrently \"node ./web/ngrok.js\" \"yarn workspace webapp dev\" -names 'ngrok,webapp'"
	},
	"devDependencies": {
		"concurrently": "^7.3.0",
		"ngrok": "^4.3.1",
		"stylelint": "^14.11.0",
		"stylelint-config-standard": "^28.0.0"
	}
}
